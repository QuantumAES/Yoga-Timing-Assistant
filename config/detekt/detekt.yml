# Поверх дефолтного набора правил detekt (buildUponDefaultConfig = true).
# Здесь только осознанные отклонения — всё остальное берётся из умолчаний.

style:
  MaxLineLength:
    maxLineLength: 120
  # В демо-данных числа — это и есть содержимое: длительности этапов
  # осмысленны сами по себе, а константы вида FIVE_MINUTES только мешают читать.
  #
  # Список исключений задаётся целиком: detekt не дополняет умолчания, а
  # заменяет их, поэтому тестовые каталоги приходится перечислять заново —
  # иначе правило включилось бы в тестах, где числа и есть ожидаемые значения.
  MagicNumber:
    excludes:
      - '**/seed/**'
      - '**/test/**'
      - '**/androidTest/**'
      - '**/commonTest/**'
      - '**/jvmTest/**'
      - '**/androidUnitTest/**'
      - '**/androidInstrumentedTest/**'

  # Ранний выход из функции по неподходящему состоянию — основной приём
  # редьюсера таймера: «команда неприменима — молча ничего не делаем».
  # Считать такие проверки за возвраты значит поощрять вложенные if-else.
  ReturnCount:
    excludeGuardClauses: true

naming:
  # @Composable-функции по соглашению именуются с заглавной буквы.
  FunctionNaming:
    ignoreAnnotated: ['Composable']
  TopLevelPropertyNaming:
    ignoreAnnotated: ['Composable']
  # Файлы *Navigation.kt намеренно держат маршрут и его NavGraphBuilder-расширение
  # рядом: это единица публичного API feature-модуля.
  MatchingDeclarationName:
    excludes: ['**/*Navigation.kt']

complexity:
  # У Composable-функций много параметров — это норма для слотов и колбэков.
  LongParameterList:
    ignoreAnnotated: ['Composable']
  # Длина Composable-функции — это размер описываемого ею дерева, а не мера
  # запутанности: форма редактора из десяти полей ровно настолько и длинна,
  # а разрезание её на «Часть1/Часть2» ухудшает читаемость, а не улучшает.
  # Сложность таких функций ловит CyclomaticComplexMethod — он остаётся включён.
  LongMethod:
    ignoreAnnotated: ['Composable']
  # Тест-класс — это набор независимых сценариев, а не объект с поведением.
  # Дробить его ради порога «не больше одиннадцати функций» значит разносить
  # связанные проверки по файлам и терять таблицу тест-плана целиком.
  #
  # ViewModel — тот же случай с другой стороны: её функции это не поведение
  # объекта, а перечень действий пользователя на экране. Их количество задано
  # экраном, и склеивать «setName» с «setCategory» в один «update» значит
  # прятать контракт экрана за словарём.
  TooManyFunctions:
    ignoreAnnotatedFunctions: ['Test', 'Preview']
    ignoreAnnotated: ['HiltViewModel']

exceptions:
  TooGenericExceptionCaught:
    active: true
