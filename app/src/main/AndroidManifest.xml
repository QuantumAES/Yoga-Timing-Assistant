<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".YtaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yta.Starting">

        <!--
            singleTop: приложение живёт в одной Activity, и ссылка на идущее
            занятие обязана приходить в уже открытое окно через onNewIntent,
            а не создавать второе.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Yta.Starting"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Ссылка на занятие: по ней уведомление в шторке открывает экран
                отсчёта, а не список профилей. Схема продублирована из
                YtaDeepLinks — в манифесте её иначе не объявить.
                BROWSABLE нет намеренно: ссылка внутренняя, снаружи открывать
                занятие некому.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:host="session"
                    android:scheme="yta" />
            </intent-filter>
        </activity>
    </application>

</manifest>
